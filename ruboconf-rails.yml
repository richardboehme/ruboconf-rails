require:
  - rubocop-rails

inherit_gem:
  ruboconf: ruboconf.yml

inherit_from:
  - cops/rails.yml
  - cops/style.yml

AllCops:
  Exclude:
    - 'db/schema.rb'
    - 'bin/{rails,bundle,rake,setup,webpack,webpack-dev-server,yarn}'
